import { HeartclickDirective } from './heartclick.directive';

describe('HeartclickDirective', () => {
  it('should create an instance', () => {
    const directive = new HeartclickDirective();
    expect(directive).toBeTruthy();
  });
});
